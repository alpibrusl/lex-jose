[package]
name        = "lex-jose"
version     = "0.1.0"
lex         = "0.10.11"
description = "JOSE for Lex: JWS / JWT / JWK / SD-JWT over std.crypto. HMAC, EdDSA and ES256, plus AP2 mandate types."
license     = "EUPL-1.2"

[dependencies]
